{
  "name": "my-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @my-project/site1 run build:server && pnpm --sequential -r build"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}